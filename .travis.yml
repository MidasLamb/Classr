language: java

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

before_install:
 - chmod +x gradlew

script:
 - ./gradlew test --tests objects.* --debug
 - ./gradlew test --tests visualobjects.* --debug
 - ./gradlew test --tests inputHandler.* --debug

jdk:
  - oraclejdk8
